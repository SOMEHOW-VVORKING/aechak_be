package com.aechak.application.payment.service

import org.springframework.stereotype.Service

/**
 * payment 도메인 비즈니스 로직 보관함 — Facade에서만 호출된다.
 * 리포지토리는 domain의 포트(PaymentRepository)를 주입받는다 — 어댑터(L3 매퍼 포함)가 생기는 시점에 연결.
 */
@Service
class PaymentService {
    // TODO: 기능 구현 시 로직 추가
}
