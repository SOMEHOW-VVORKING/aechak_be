package com.aechak.application.product.usecase

/**
 * product 도메인의 유일한 진입점 계약. 규칙은 user 도메인 템플릿(UserUseCase) 참조.
 * 입출력 어휘(command/·result/·query/)는 이 패키지 하위에 둔다 — 계약은 usecase/, 구현은 service/.
 */
interface ProductUseCase {
    // TODO: 기능 확정 시 메서드 추가 — 상품 등록/수정, 옵션·재고, 찜, 상세 등
}
