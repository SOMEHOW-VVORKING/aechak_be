# 애착(aechak) 백엔드 — 팀 공통 에이전트 규칙

Kotlin + Spring Boot 멀티모듈 백엔드. 아키텍처·계층 규칙의 정본은 이 레포의 `docs/`(`00-overview.md` ~ `80-product-catalog.md`) — 코드 작성 전 해당 계층 문서를 먼저 읽는다.

## PR·머지 가드레일 (필수 — 위반 금지)

- **PR 생성 전 반드시 사람 확인을 받는다.** 대상 브랜치·커밋 목록·PR 제목(`[SCRUM-번호] 작업 내용`)·본문(레포 PR 템플릿)을 먼저 보여주고, 명시적 승인이 있을 때만 `gh pr create` 등 PR 생성 도구를 실행한다. 승인 없이 PR을 만들지 않는다.
- **PR 머지는 절대 에이전트가 하지 않는다.** 머지는 리뷰(1인 이상) 후 사람이 직접 한다. `gh pr merge`·GitHub API 머지 호출 금지.
- **요청 범위만 수행한다.** "오류 잡아줘"는 코드 수정까지다 — 커밋·push·PR 생성은 각각 별도 지시가 있을 때만 진행한다.
- **PR 생성 시점에 `docs/` 갱신 여부를 함께 확인한다.** 구현하면서 문서에 적힌 계층 규칙·모듈 구조·정책과 달라진 부분이 생겼으면 문서 수정을 같은 PR에 포함하고, 무엇이 어떻게 바뀌었는지 PR 본문에 적는다. 문서 반영을 다음 PR로 미루지 않는다.
  - 구현이 문서와 어긋나는데 **어느 쪽이 맞는지 판단이 서지 않으면 임의로 고치지 말고 사람에게 보고한다.** 구현이 맞으면 문서를, 문서가 맞으면 구현을 고치는 것이 원칙이다.
- `develop`·`main`에 직접 push 금지. 커밋·PR에 Co-Authored-By 등 AI 흔적을 남기지 않는다.

## 레포 구성 (GitHub org: `SOMEHOW-VVORKING`)

| 레포 | 역할 |
|---|---|
| `aechak_be` | 백엔드 (이 레포) |
| `aechak_fe` | 프론트엔드 — API 연동 시 계약(contracts) 기준으로 맞춘다 |

## 작업 단위·브랜치

- **작업 착수 전 Jira(SCRUM) 티켓을 먼저 발행한다.** 기존 티켓의 후속 작업이라도 범위가 다르면 새 티켓을 판다. 티켓 번호 없이 브랜치를 파거나 커밋하지 않는다.
- 브랜치는 `develop` 기준으로 티켓 하나당 하나 — `prefix/SCRUM-번호-작업내용` (예: `feature/SCRUM-59-plan-git-strategy`). PR 대상은 `develop`.
- 브랜치 prefix: `feature`(신규 기능) · `fix`(버그 수정) · `hotfix`(운영 장애 긴급 수정, `main` 기준) · `refactor` · `chore`(설정·의존성) · `docs` · `test`
- feature 브랜치는 생명주기를 짧게 유지해 `develop`과 벌어지지 않게 한다.

## 커밋 규칙

형식은 `[SCRUM-번호] type: 작업 내용`.

```
[SCRUM-55] feat: Redis 분산 락 구현
```

커밋 타입:

| Type | 용도 |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 기능 변경 없는 코드 개선 |
| `style` | 코드 포맷 수정 |
| `chore` | 설정 및 기타 작업 (빌드·의존성 포함) |
| `docs` | 문서 수정 |
| `test` | 테스트 코드 추가 및 수정 |
| `perf` | 성능 개선 |
| `deploy` | 배포 관련 작업 |

커밋 본문:

- 제목 줄은 무엇을 바꿨는지만 적고, **왜 바꿨는지가 제목으로 설명되지 않으면 본문에 쓴다.** 제목과 본문 사이는 빈 줄로 띄운다.
- 본문에 적을 것: 변경 이유, 재현되던 문제, 함께 바꿔야 했던 부수 변경. 코드를 봐도 알 수 없는 맥락만 남긴다.

```
[SCRUM-40] fix: 연필 그리기 중단 문제 수정

연필 그리기 과정에서 발생하던 메모리 누수 문제를 수정함.
```

- 커밋은 의미 있는 작업 단위로 자른다. 에이전트가 만든 변경이라 분리가 어려운 경우에도 한 커밋의 범위가 지나치게 커지지 않게 관리한다.

## 주석·설명을 남기는 위치

- **코드 주석에는 그 코드가 무엇을 하는지만 짧게 적는다.** 한두 줄이면 충분하고, 코드를 읽으면 바로 보이는 내용은 주석으로 반복하지 않는다.
- **설계 판단·검토한 대안·트레이드오프는 주석이 아니라 PR 본문에 남긴다.** "왜 A 대신 B를 골랐는지"는 리뷰어가 읽을 자리이지, 나중에 코드와 어긋날 자리가 아니다.
- 주석에 외부 문서의 섹션 좌표(`docs/xx §y` 등)를 박아 넣지 않는다. 문서가 개편되면 그대로 거짓말이 된다.
